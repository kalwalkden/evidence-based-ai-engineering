# Tasks

- [x] `001-update-discover-architecture` — Adopt Reveal 0.122.0 adapters and encode verified safety
  gates in repository discovery.
- [x] `002-update-trace-domain-flow` — Start traces with `trace://` while preserving manual runtime
  verification.
